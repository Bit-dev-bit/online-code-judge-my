[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

[build.environment]
  # Provide your backend URL here when deploying in Netlify UI
  # VITE_API_URL = "https://your-render-backend-url.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
